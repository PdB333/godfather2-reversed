// FUNC_NAME: executeRepeatedly
void executeRepeatedly(int count)
{
    if (count > 0) {
        do {
            FUN_00535940();  // called function: processItem
            count--;
        } while (count != 0);
    }
    return;
}